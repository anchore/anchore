_anchore_completion() {
    COMPREPLY=( $( env COMP_WORDS="${COMP_WORDS[*]}" \
                   COMP_CWORD=$COMP_CWORD \
                   _ANCHORE_COMPLETE=complete $1 ) )
    return 0
}

complete -F _anchore_completion -o default anchore;
